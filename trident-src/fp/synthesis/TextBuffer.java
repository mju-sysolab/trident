/*
 *
 LA-CC 05-135 Trident 0.7.1

Copyright Notice
Copyright 2006 (c) the Regents of the University of California.

This Software was produced under a U.S. Government contract
(W-7405-ENG-36) by Los Alamos National Laboratory, which is operated
by the University of California for the U.S. Department of Energy. The
U.S. Government is licensed to use, reproduce, and distribute this
Software. Permission is granted to the public to copy and use this
Software without charge, provided that this Notice and any statement
of authorship are reproduced on all copies. Neither the Government nor
the University makes any warranty, express or implied, or assumes any
liability or responsibility for the user of this Software.


 */


package fp.synthesis;

public class TextBuffer { }
/*
extends StringBuffer implements Text {
  
  private String _prefix;

  public TextBuffer(String start, String prefix) {
    super(start);
    _prefix = prefix;
  }

  public String toText(String prefix) {
    return prefix+toString();
  }

  public void appendLong(String name, long value) {
    append(_prefix);
    append(TAB);
    append(START);
    append(name);
    append(SPACE);
    append(value);
    append(END);
    append(LINE);
  }

  public void appendInt(String name, int value) {
    append(_prefix);
    append(TAB);
    append(START);
    append(name);
    append(SPACE);
    append(value);
    append(END);
    append(LINE);
  }

  public void appendStr(String name, String value) {
    append(_prefix);
    append(TAB);
    append(START);
    append(name);
    append(SPACE);
    append(value);
    append(END);
    append(LINE);
  }

}
*/
